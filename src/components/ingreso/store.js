import D from "debug";

const debug = D("ciris:storeIngreso");


const state = {
};

const actions = {
};

const mutations = {
};

const getters = {
};

const store = {
  state,
  actions,
  mutations,
  getters,
  namespaced: true,
};

debug("Store inicializado");
export default store;
